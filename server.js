// ===========================================
// Clasiccal function for getting a pseudo-random number between a min and max value
function randomNumber(min, max) { 
    return Math.random() * (max - min) + min;
}
