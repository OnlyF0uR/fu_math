// ===========================================
// Clasiccal function for getting a pseudo-random number between with a min and max
function randomNumber(min, max) { 
    return Math.random() * (max - min) + min;
}